$('#new_comment').on('ajax:success', function() {
	$('#comment-<%= @comment.id %>').remove();
	$(this).before('<%= j render(@comment) %>');
	$(this).remove();
	$('#comment-content<%= @comment.id %>').html();
})

