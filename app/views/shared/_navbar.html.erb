<nav class=" navbar-fixed-top" id="navbar-main" >
 
	<div class="container">
    
		<div class="navbar-header flex ">
      <%= image_tag 'smart-logo.png', class: "smart-logo navbar-logo"%>
      <%= link_to "Smartgamer", root_path, class: "navbar-logo nav-text navbar-title" %>
      
      <!-- Searchbar -->
      <div id="custom-search-input">
        <%= form_tag search_path, method: 'get' do %>
          <div class="input-group">
            <!-- FIXME - change message from "Please fill out this field." -->
            <%= text_field_tag :search, params[:search], placeholder: 'Search for players or articles', required: true, class: 'form-control' %>
            <!-- FIXME - Change Go! to search icon -->
            <span class="input-group-btn"><%= image_submit_tag("search.png", class: 'btn btn-default search') %></span>
          </div>
        <% end %>
      </div>
      


      <%= link_to "Create Articles", articles_path, class: "navbar-brand nav-text" if current_user && current_user.admin?%>
		</div>

		<div class="collapse navbar-collapse " id="navbar">
      <% unless signed_in?  %>
        <ul class="navbar-nav pull-right   navbar-signin">
   
          <ul> <%= image_tag('steam-icon-large.png', class: "steam_log_in")%><%= link_to "Sign in with steam", '/auth/steam', class: "navbar-title nav-text" %></ul>
        </ul>
      <% end %>
			<ul class="nav navbar-nav navbar-right">
					<li class="dropdown">
						<% if signed_in? && current_user.nickname != nil %>
						<%= link_to "My Profile", current_user, role: "button", class:'nav-text'%>

						<% end %>

					</li>
        <!-- messages>
				<li>
					<a href="#"><i class="fa fa-envelope-o"></i> Messages</a>
				</li>
        <!-->

			</ul>
		</div>
  </div>
	
</nav>
